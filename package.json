{
  "name": "korean-llm-exercises",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "demo": "npx tsx scripts/demo.ts",
    "build": "npm run build -w packages/core",
    "typecheck": "npx tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
